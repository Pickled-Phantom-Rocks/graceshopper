import React from 'react';
import {fetchproducts} from '.'

const AddProduct = (props) => {
	const {baseURL} = props;

	//fetch the list of products
	//when a product is selected, fetch the categories minus any categories the item is in
	//select category
	//create category_product with the productID and categoryID



	return <div className="form">
		<h3>Add a Product to a Category</h3>
		<br/>
		<form>

		</form>
	</div>
}

export default AddProduct;